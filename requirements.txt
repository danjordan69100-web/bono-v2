# Bono V2 — runtime dependencies (pinned versions tested 17/05/2026)
# Pin majeur+mineur pour éviter les breaking changes silencieux (cf. bug Deepgram v3->v4 du 17/05).

# --- LLM / STT / TTS ---
anthropic==0.86.0
# CRITICAL : deepgram-sdk v4+ change l'API (listen.v1.media vs listen.rest.v("1")).
# Si tu upgrades, audit core_service.py:1218-1227 d'abord.
deepgram-sdk==3.11.0
faster-whisper==1.2.1

# --- Audio I/O ---
sounddevice==0.5.5
miniaudio==1.71
pygame==2.6.1

# --- Server / API ---
fastapi==0.135.1
uvicorn==0.41.0
httpx==0.28.1
httpx-sse==0.4.3
websockets==16.0
aiohttp==3.13.2
requests==2.32.5

# --- IPC ---
pyzmq==27.1.0

# --- Telemetry / system ---
psutil==7.2.2
pynvml==13.0.1
numpy==2.4.4

# --- Logging / config ---
loguru==0.7.3
python-dotenv==1.2.2
